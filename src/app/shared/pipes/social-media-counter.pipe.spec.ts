import { SocialMediaCounterPipe } from './social-media-counter.pipe';

describe('SocialMediaCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialMediaCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
