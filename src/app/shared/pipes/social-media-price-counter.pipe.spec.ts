import { SocialMediaPriceCounterPipe } from './social-media-price-counter.pipe';

describe('SocialMediaPriceCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialMediaPriceCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
